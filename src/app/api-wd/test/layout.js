export const metadata = {
  title: 'Workflow Viewer Test | OD2',
  description: 'Test page for the OD2 Workflow Viewer component with JSON export functionality',
};

export default function TestLayout({ children }) {
  return children;
}
