# MongoDB Configuration for Connection Optimization
# Add these to your .env.local file

# Your existing MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# Optional: Connection pool settings (these are now set in code)
# MONGODB_MAX_POOL_SIZE=10
# MONGODB_SERVER_SELECTION_TIMEOUT=5000
# MONGODB_SOCKET_TIMEOUT=45000
# MONGODB_MAX_IDLE_TIME=30000

# Application settings
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Admin authentication (change the password for security)
# ADMIN_PASSWORD=od2admin123